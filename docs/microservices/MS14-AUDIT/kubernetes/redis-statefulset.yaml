apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ms14-audit-redis
  namespace: zenia
  labels:
    app: ms14-audit-redis
spec:
  serviceName: ms14-audit-redis
  replicas: 1
  selector:
    matchLabels:
      app: ms14-audit-redis
  template:
    metadata:
      labels:
        app: ms14-audit-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ms14-audit-secrets
              key: redis-password
        command: ["redis-server", "--requirepass", "$(REDIS_PASSWORD)"]
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        volumeMounts:
        - name: redis-data
          mountPath: /data
      volumes:
      - name: redis-data
        persistentVolumeClaim:
          claimName: ms14-audit-redis-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms14-audit-redis-pvc
  namespace: zenia
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
