apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ms16-registry-redis
  namespace: zenia
  labels:
    app: ms16-registry-redis
spec:
  serviceName: ms16-registry-redis
  replicas: 1
  selector:
    matchLabels:
      app: ms16-registry-redis
  template:
    metadata:
      labels:
        app: ms16-registry-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ms16-registry-secrets
              key: redis-password
        command: ["redis-server", "--requirepass", "$(REDIS_PASSWORD)"]
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        volumeMounts:
        - name: redis-data
          mountPath: /data
      volumes:
      - name: redis-data
        persistentVolumeClaim:
          claimName: ms16-registry-redis-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms16-registry-redis-pvc
  namespace: zenia
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
